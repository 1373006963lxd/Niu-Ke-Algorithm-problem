package com.lxd.niukework.WeiNing;

/**
 * @auther li xiao dong
 * @date 2020/3/22 0022 下午 17:19
 */
public class MySql {
//    同一个ID可能有1条记录，也可能有多个记录，写出把所有重复的ID查出来
    /*
SELECT ID, COUNT(ID)
FROM 你的表
GROUP BY ID
HAVING COUNT(ID) > 1
    * */
}
